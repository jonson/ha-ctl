# Home Assistant connection
HA_URL=http://homeassistant.local:8123
HA_TOKEN=your-long-lived-access-token

# Safe entity for integration tests (must exist, will be toggled on/off)
HA_TEST_ENTITY=input_boolean.test_toggle
